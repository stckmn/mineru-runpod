---
title: Overview
description: Architecture, what's in the repo, supported workloads, and how it compares to Marker, GROBID, and Nougat.
---

`runpod-mineru` is a generic, reusable [MinerU 2.5](https://github.com/opendatalab/MinerU) PDF-parsing service running on [RunPod Serverless](https://runpod.io?ref=31jdfpnq). The repo deliberately knows nothing about any specific downstream system — it ships a worker image, a small Python client, and the deploy / destroy glue. Anything that needs `PDF → structured Markdown + JSON` (a RAG pipeline, a document indexer, an Office-doc archive) calls it the same way.

## What's in the repo

- **`handler.py`** — the serverless worker. Accepts a PDF via URL, base64, or mounted-volume path; calls MinerU's async parse; returns Markdown + `content_list` + `middle.json` + images.
- **`mineru_client/`** — the Python package consumers import. One class (`MineruClient`), two methods (`parse_pdf`, `parse_pdf_from_file`). Pure-Python; imports nothing GPU- or MinerU-related, so it's safe to depend on from any caller.
- **`deploy.py` / `destroy.py`** — stand up and tear down the RunPod endpoint from CLI flags. Every dashboard setting is exposed as a flag.
- **`.runpod/hub.json`** — RunPod Hub listing metadata (title, description, GPU pool list, CUDA versions).
- **`examples/`** — `parse_url_example.py`, `parse_b64_example.py`, and a `parser_adapter_example.py` showing how to wrap MinerU output in your own typed domain model.

## Who it's for

| If you're building... | What you get |
|---|---|
| **Office document indexing** (Word / PowerPoint / Excel exported to PDF) | Spiky ingest, pay only during bursts; preserves tables + figures |
| **Document RAG pipelines** | Section-aware chunks with page provenance out of the box |
| **Contract / spec / standards parsing** | Handles long attribute tables and cross-page constructs |
| **Invoice / receipt extraction** | Table fidelity + image extraction in one pass |
| **Multi-language documents** | MinerU 2.5 supports 84 languages, including handwriting |

## Why MinerU + serverless

- **Accuracy.** MinerU 2.5's 1.2B VLM leads the [OmniDocBench](https://github.com/opendatalab/OmniDocBench) leaderboard on text, formula, table, and reading-order metrics — see the [HuggingFace model card](https://huggingface.co/opendatalab/MinerU2.5-Pro-2604-1.2B) and the [technical report](https://arxiv.org/abs/2604.04771).
- **Economics.** Per-second billing on RunPod with FlashBoot means an idle worker costs nothing. ~$0.0001 per page on a 24 GB Ampere GPU at $0.44/h.
- **Licensing.** MinerU is Apache 2.0 with explicit commercial thresholds (free below 100M MAU and $20M monthly revenue, with attribution). Among open-source GPU-class PDF parsers, that's the cleanest license for production SaaS use.

## How it compares

| | runpod-mineru | Marker | GROBID | Nougat |
|---|---|---|---|---|
| Scale-to-zero | ✅ | ⚠️ possible | ❌ always-on | ❌ |
| GPU | required | CPU or GPU | CPU | required |
| Tables | ✅ structured | ⚠️ noisy | ⚠️ refs only | ⚠️ |
| Equations | ✅ LaTeX | ✅ LaTeX | ❌ | ✅ LaTeX |
| Multi-lang | ✅ 84 | ⚠️ Latin-heavy | EN only | EN/limited |
| License | Apache 2.0 + thresholds | **GPL + Rail-M** | Apache 2.0 | MIT code, **CC-BY-NC weights** |
| Commercial SaaS | ✅ | ⚠️ paid license | ✅ | ❌ blocked |

See the [project README](https://github.com/sergeyshmakov/runpod-mineru#how-does-it-compare) for the source-cited version of this table.

## Next steps

There are two paths from here, depending on whether you want to run the official image as-is or run your own customised build:

**Easiest — deploy from the RunPod Hub.** Sign up via [this referral link](https://runpod.io?ref=31jdfpnq), open **The Hub → Serverless repos**, find `runpod-mineru`, click Deploy. Grab the endpoint id and you're parsing PDFs.

**Customise — fork and auto-build.** Fork [the repo](https://github.com/sergeyshmakov/runpod-mineru), then in the RunPod dashboard do **The Hub → Serverless repos → Import Git Repository** pointed at your fork. RunPod builds the image on every push to `main`. Use this path if you need to pin different MinerU / vLLM versions or modify `handler.py`.

Either way, total wall time is roughly 10 minutes assuming RunPod has spare capacity. See the [project README's Deploy section](https://github.com/sergeyshmakov/runpod-mineru#deploy) for the full step-by-step.
